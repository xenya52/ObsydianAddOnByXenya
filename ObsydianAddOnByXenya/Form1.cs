namespace ObsydianAddOnByXenya
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            //"Form1" Should have 4 buttopns (open foulder, syncGit, readme, settings)
            //Class settings (additional setting eventualy a snyc via. google should be possible too..)
            //Class Git (void Pushing method)
            //Class Obsidian(connecting obisdian and the folder with the program)
            //Class readme (to open the readme)
            //Class execution (the execution of the programm should be separated in the class)
            InitializeComponent();
        }

        private void Form1_Load(object sender, EventArgs e)
        {

        }
    }
}